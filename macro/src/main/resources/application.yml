invest:
  zhi-pu-ai:
    api-key: ${ZHI_PU_AI_API_KEY}
    model: glm-4.7-flash
server:
  servlet:
    context-path: /invest/macro/api
spring:
  mail:
    host: smtp.163.com
    password: ${QAZCXH_163_COM_MAIL_PASSWORD}
    username: qazcxh@163.com
  profiles:
    active: chixuehui
---
server:
  port: 6012
spring:
  config:
    activate:
      on-profile: chixuehui
    import:
      - file:${user.home}/IdeaProjects/ricewines/local-config/common-chixuehui.yml
---
spring:
  config:
    activate:
      on-profile: chixuehuidocker
    import:
      - file:/deploy/chixuehuidocker/common-chixuehuidocker.yml